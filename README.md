split_commands() : We split the list into commands and execute them by ex.

ex() : ex determines the type of command and executes.

cd() : For ~ it changes to the invoking directory. The path of this directory is a parameter.

ls list all files. It has -l and -a switches.

handler() : Handler is used to find if termination of any background process has occured.

modify_path() : It changes the path prefix 

pwd - results the current working directory